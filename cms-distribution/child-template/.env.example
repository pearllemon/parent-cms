# Required: switches the CMS into child mode so Distribution screens
# (Releases, Installations, Upgrade Log, API Registry, Signing Keys,
# Setup Wizard) are hidden from the admin UI.
VITE_CMS_MODE=child